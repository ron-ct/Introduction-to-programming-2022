/*C program that prints the numbers 1 to 20 using for*/
#include <stdio.h>
#include <stdlib.h>

int count;

int main()
{
    for (count = 1; count <= 20; count++)
        printf("%d\n",count);
    return 0;
}
